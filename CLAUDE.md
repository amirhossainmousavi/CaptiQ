# CaptiQ

## گراف دانش (Graphify)

گراف دانش این پروژه در `graphify-out/` ساخته شده (`graph.json`).

برای سوالات مربوط به ساختار کد، روابط بین ماژولها، و معماری — **اول از گراف استفاده کن**، با دستور:

```bash
graphify query "<سوال>"
```

- این قوانین را دنبال کن: با گراف شروع کن، نه با خواندن مستقیم فایلها.
- هر رابطهای در گراف، برچسب `EXTRACTED` (از AST) یا `INFERRED` دارد.
- نبود فایل گراف: اگر `graphify-out/graph.json` فایل موجود نبود، آن را بساز (با `/graphify` یا مراحل pipeline در SKILL گرافیفای).